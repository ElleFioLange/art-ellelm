Art Portfolio Website for Elle Fiorentino-Lange

Notes for those digging under the hood:

- I use `const state = useState(*value*)` and subsequently use `state[0]` to access the value and `state[1]` as the setter.
